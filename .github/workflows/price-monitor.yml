name: Price Monitor Test

on:
  repository_dispatch:
    types: [price-check]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Send test notification
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
        run: |
          curl \
            -H "Title: Price Monitor Test" \
            -H "Priority: default" \
            -H "Tags: shopping_cart" \
            -d "External cron successfully triggered GitHub Actions." \
            "https://ntfy.sh/$NTFY_TOPIC"
